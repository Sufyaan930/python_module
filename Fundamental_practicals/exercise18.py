#Write a program that creates a list of five numbers entered by the user, then calculates and prints the sum and average of these numbers.

mylist = [10,20,30,40,50]
sum = sum(mylist)
average = sum / len(mylist)
print(f"The sum of the numbers is {sum} and the average is {average}")
